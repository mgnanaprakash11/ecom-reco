name: ecom_reco
version: 1.0.0
profile: ecom_reco
model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecom_reco:
    +materialized: view
    staging:
      +schema: stg
    marts:
      +schema: marts
    logs:
      +materialized: table
