version: 2

models:
  - name: orders_processing_logs
    description: "Summaries of raw order uploads generated by dbt to confirm the pipeline end-to-end."
    columns:
      - name: processed_at
        tests:
          - not_null
      - name: dbt_invocation_id
        tests:
          - not_null
